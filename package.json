{
  "name": "streamfart-bridge",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "axios": "^1.7.0",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  },
  "scripts": {
    "build": "tsc api/webhook.ts --outDir dist --module nodenext --target es2022",
    "test": "bash scripts/test_dispatch.sh"
  }
}
